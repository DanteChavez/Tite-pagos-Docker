export interface PaymentNotifier {
  // TODO: Implementar notificaciones específicas por proveedor
  // - sendPaymentConfirmation(paymentId: string): Promise<void>
  // - sendPaymentFailure(paymentId: string, reason: string): Promise<void>
  // - sendRefundNotification(paymentId: string): Promise<void>
  // - handleWebhookNotification(webhookData: any): Promise<void>
}
