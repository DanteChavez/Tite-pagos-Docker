import { Payment } from '../../domain/entities/payment.entity';

export interface PaymentProcessor {
  // TODO: Implementar métodos para procesamiento de pagos
  // - processPayment(paymentData: any): Promise<Payment>
  // - refundPayment(paymentId: string, amount?: number): Promise<Payment>
  // - cancelPayment(paymentId: string): Promise<Payment>
  // - getPaymentStatus(paymentId: string): Promise<string>
  // - handleWebhook(webhookData: any): Promise<void>
}
