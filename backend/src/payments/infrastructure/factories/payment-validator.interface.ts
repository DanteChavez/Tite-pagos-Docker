export interface PaymentValidator {
  // TODO: Implementar validaciones específicas por proveedor
  // - validatePaymentData(paymentData: any): Promise<boolean>
  // - validateAmount(amount: number, currency: string): boolean
  // - validatePaymentMethod(paymentMethod: any): boolean
  // - validateWebhookSignature(signature: string, payload: string): boolean
}
